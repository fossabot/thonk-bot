# fuck travis ci
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.9.4-stretch
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          command: npm test
